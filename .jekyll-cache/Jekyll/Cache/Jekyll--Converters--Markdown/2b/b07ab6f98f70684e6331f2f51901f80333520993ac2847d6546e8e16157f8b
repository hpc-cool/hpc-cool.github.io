I"�<p>我在协助小伙伴调试一个只有在 GitHub 的 Action 自动测试时才会炸的问题，而我发现默认的控制台输出是不会在 GitHub 的 Action 显示的，换句话说，在使用 dotnet test 时，代码里面使用的控制台输出不会进行输出</p>

<!--more-->

<!-- CreateTime:2020/11/28 9:02:42 -->

<p>解决方法很简单，只需要在控制台输出的部分逻辑修改为 <code class="language-plaintext highlighter-rouge">Console.WriteLine</code> 而不是 <code class="language-plaintext highlighter-rouge">Debug.WriteLine</code> 方法</p>

<p>然后在 dotnet test 的命令加上 <code class="language-plaintext highlighter-rouge">-l "console;verbosity=detailed"</code> 代码，如下面代码</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>dotnet test --configuration release -l "console;verbosity=detailed"
</code></pre></div></div>

<p>这样就能在 GitHub 的 Action 进行单元测试时，输出对应的日志</p>

<p>为什么 <code class="language-plaintext highlighter-rouge">Debug.WriteLine</code> 方法没有输出？原因是 <code class="language-plaintext highlighter-rouge">--configuration release</code> 配置了不要让 Debug 下输出</p>

<p>更多请看 <a href="https://github.com/microsoft/vstest/issues/799">Console.WriteLine calls during dotnet test are not emitted to the console on Windows · Issue #799 · microsoft/vstest</a></p>

:ET