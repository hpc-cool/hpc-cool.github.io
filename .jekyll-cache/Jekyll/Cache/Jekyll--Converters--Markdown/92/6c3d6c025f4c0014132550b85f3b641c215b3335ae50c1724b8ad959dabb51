I"�	<p>因为我现在的分支是的名很长，每次需要上次当前分支需要写很多代码，是不是有很简单方法上传当前分支。</p>

<!--more-->

<!-- CreateTime:2019/8/31 16:55:59 -->

<!-- 标签：git -->

<p>如果要上传一个分支到仓库 origin 那么就需要使用下面的命令</p>

<div class="language-csharp highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="n">git</span> <span class="n">push</span> <span class="n">origin</span> <span class="err">分支</span>
</code></pre></div></div>

<p>我存在下面几个分支</p>

<div class="language-csharp highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="n">t</span><span class="p">/</span><span class="n">lindexi</span><span class="p">/</span><span class="n">Avalonial_Grid_Arrange</span>
 <span class="n">t</span><span class="p">/</span><span class="n">lindexi</span><span class="p">/</span><span class="n">Avalonial_Grid_Infinity</span>
</code></pre></div></div>

<p>我在上传的时候需要写很多代码，至少很难用 tab 出我现在的分支</p>

<p>我在网上找到一个方法，用来上传当前的分支</p>

<div class="language-csharp highlighter-rouge"><div class="highlight"><pre class="highlight"><code>  <span class="n">git</span> <span class="n">push</span> <span class="n">origin</span> <span class="n">HEAD</span>
</code></pre></div></div>

<p>但是我发现每次这样写还是很长，再告诉大家一个方法</p>

<div class="language-csharp highlighter-rouge"><div class="highlight"><pre class="highlight"><code> <span class="n">git</span> <span class="n">config</span> <span class="p">--</span><span class="k">global</span> <span class="n">push</span><span class="p">.</span><span class="k">default</span> <span class="n">current</span>
</code></pre></div></div>

<p>设置默认使用<code class="language-plaintext highlighter-rouge">git push </code>就是上传当前分支</p>

<p>可以设置 push.default 的值为</p>

<ul>
  <li>nothing  不上传任何分支</li>
  <li>matching 上传所有分支</li>
  <li>upstream/tracking  上传当前跟踪分支</li>
  <li>current  上传当前分支</li>
</ul>

<p>实际上还有更多，请看<a href="https://git-scm.com/docs/git-config.html#git-config-pushdefault">Git - git-config Documentation</a></p>

<p>https://stackoverflow.com/q/14031970/6116637</p>

<p><img src="https://i.loli.net/2018/05/19/5affbfbc1926d.jpg" alt="" /></p>

:ET